// @wolfram77
public class MessageList extends Thing {
  
  // MessageList (x, y, width, height, clr)
  // - create a message list
  public MessageList(float x, float y, float width, float height, color clr) {
    super(x, y, width, height, clr);
  }
}

